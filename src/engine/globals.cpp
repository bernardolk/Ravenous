const extern size_t   COLLISION_LOG_BUFFER_CAPACITY = 150;
const extern size_t   COLLISION_LOG_CAPACITY = 20;
const extern size_t   COLLISION_BUFFER_CAPACITY = WORLD_CELL_CAPACITY * 8;
const extern size_t   MESSAGE_BUFFER_CAPACITY = 10;
const extern int      MAX_MESSAGES_TO_RENDER = 8;