- use killsymbolserver.bat when the linker complains about nonsense

- put startup_ravenous.bat at C:\Users\youruser\AppData\Roaming\Microsoft\Windows\startup
 to assign project directory to the drive letter w:

- build.bat builds project

- run.bat executes project .exe

- init.bat can be executed by a terminal on startup to setup the vcc env dev variables and cd to
the project directory (under tools)